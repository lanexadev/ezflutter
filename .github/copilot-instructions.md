# GitHub Copilot adapter

Read and follow the root `AGENTS.md` as the canonical START.DART agent contract. Load applicable `.agents/rules/` and the matching `.agents/skills/*/SKILL.md` progressively. This file intentionally defines no separate engineering doctrine.
