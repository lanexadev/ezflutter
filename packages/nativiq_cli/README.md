# nativiq_cli

The deterministic command-line interface for creating and evolving Nativiq Flutter applications.

```bash
dart pub global activate nativiq_cli
nativiq doctor
nativiq create my_app
nativiq add feature checkout
```

Commands are non-interactive and support `--json`. Generated projects target Android and iOS and include a feature-first MVVM architecture plus an AI-agent contract.
