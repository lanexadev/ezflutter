# start_dart_cli

The deterministic command-line interface for creating and evolving START.DART Flutter applications.

```bash
dart pub global activate start_dart_cli
startdart doctor
startdart create my_app
startdart add feature checkout
```

Commands are non-interactive and support `--json`. Generated projects target Android and iOS and include a feature-first MVVM architecture plus an AI-agent contract.
