# Claude Code adapter

Read and follow `AGENTS.md`, the canonical Nativiq agent contract. Load only the applicable files under `.agents/rules/` and invoke the matching repository skill under `.agents/skills/`. This adapter intentionally contains no duplicated doctrine.
