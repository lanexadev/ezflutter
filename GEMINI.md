# Gemini adapter

Read and follow `AGENTS.md`, the canonical Nativiq agent contract. Load applicable `.agents/rules/` and `.agents/skills/` progressively. This adapter intentionally contains no duplicated doctrine.
